#include <stdio.h>

int max 3(int a, int b, int c, larger);

int main (void)
{
	int a, b, c, i;
	
	printf("Enter First Number: \n");
	scanf("%d", &a);
	printf("Enter Second Number: \n");
	scanf("%d", &b);
	printf("Enter Thrid Number: \n");
	scanf("%d", &c);
	printf("\nThe First Number Is: %d\n", a);
	printf("The Second Number Is: %d\n", b);
	printf("The Thrid Number Is: %d\n", c);
	
	i = = max.3(a,b,c);
	printf("largest = %d", i);
}

int = max.3(int a, int b, int c)
		{int larger, largest;
		if (a>b)
			larger = a;
		else
			larger = b;
	printf("Larger Between %d and %d is %d", a, b, larger);
		if(c>larger)
			larger = c;
		else
			largest = larger;
		return(larest);
	}

